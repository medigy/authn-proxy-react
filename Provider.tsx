import { Config } from "./webpack.config";
// import PropTypes from "prop-types";

export const AuthnProxyReactProvider = (config) => {
  console.log("config", config);

};

// AuthnProxyReactProvider.propTypes = {
//   config: PropTypes.object,
// };
